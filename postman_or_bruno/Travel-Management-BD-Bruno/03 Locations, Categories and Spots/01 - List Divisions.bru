meta {
  name: List Divisions
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/locations/divisions
  body: none
  auth: none
}

tests {
  test("Status code is successful", function () {
    expect(res.status).to.be.oneOf([200, 201]);
  });

  test("Response has success=true", function () {
    expect(res.body.success).to.equal(true);
  });
}
